import React from 'react';
import NoiseTracker from './NoiseTracker';

const App = () => {
    return (
        <div style={{ display: "flex", alignContent: "center", alignItems: "center" }}>
            <NoiseTracker />
        </div >
    );
};

export default App;
